conf:
  logging:
    category: ingesticks
    location: /var/log/saifu/ingesticks
    level: DEBUG
    format: '%(asctime)s [%(levelname)s] [@%(name)s] %(message)s'
  app:
    exchange: mktaggupd
    database:
      host: saifudb
      database: saifudb
      credentials:
        username: saifudb
        password: saifudb
    mq:
      host: rmq
      credentials:
        username: guest
        password: guest
