conf:
  log:
    category: ingesticks
    location: /var/log/saifu/ingesticks
    level: DEBUG
    format: '%(asctime)s [%(levelname)s] [@%(name)s] %(message)s'
  app:
    db:
      host: saifudb
      schema: saifudb
      creds:
        username: saifudb
        password: saifudb
    mq:
      host: rmq
      exchange: mktaggupd
      creds:
        username: guest
        password: guest
